export type InvoiceItem = {
  name: string;
  price: number;
  qty: number;
  [key: string]: any;
};

export type ClientInfo = {
  name: string;
  email: string;
  phoneNumber: string;
  [key: string]: any;

};
